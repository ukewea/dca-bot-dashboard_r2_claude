# Copy one of these into your local env or CI as needed

# Development (local dev server)
# VITE_BASE_PATH=/

# GitHub Pages (project pages under subpath)
# VITE_BASE_PATH=/dca-bot-dashboard_r2_claude/

# Cloudflare Pages (served at domain root)
# VITE_BASE_PATH=/

# Custom domain (served at domain root)
# VITE_BASE_PATH=/

# Optional explicit data path override (else defaults to ${BASE_URL}data)
# VITE_DATA_BASE_PATH=/data
